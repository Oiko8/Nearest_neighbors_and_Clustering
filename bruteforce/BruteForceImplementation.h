#include <iostream>
#include <vector>
#include <cmath>
#include <algorithm>

using namespace std;

vector <pair<double,int>> brute_force_search(const vector<vector<double>> &pts, vector<double> q, int N);